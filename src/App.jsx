/**
 * DEPRECATED — superseded by src/app/App.jsx + src/app/router/AppRouter.jsx.
 * Kept only as a re-export so any stray imports of "./App" still resolve.
 * Safe to delete once you confirm nothing imports it.
 */
export { default } from "./app/App";
