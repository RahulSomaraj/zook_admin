# Copy to .env and adjust. Vite only exposes vars prefixed with VITE_.

# Base URL of the backend API.
VITE_API_URL=/api

# Set to "true" to run the UI against local mock data with no backend
# (login accepts any email/password, data comes from src/data).
VITE_USE_MOCK=false
